<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- By changing the parent to Theme.Material3.Light.NoActionBar, the app will always use the light theme. -->
    <style name="Base.Theme.MyJournalAppFinal" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.MyJournalAppFinal" parent="Base.Theme.MyJournalAppFinal" />
</resources>
